---
version: alpha
name: Мастерская — присутствие
description: >-
  Мягкий белый холст, графит вместо чёрного, блоки выделяются тоном, а не рамкой
  и не тенью. Зелёный ведёт к действию, слива оживляет, синий уводит по ссылке,
  красный оставлен ошибке. Концептуально Material 3: высота передаётся цветом
  поверхности, а не тенью. Шкала весов вместо единственного, предложный регистр
  вместо капса. Система собрана под один продукт — мебель на заказ, где человек
  отдаёт незнакомому сервису свой телефон и должен при этом чувствовать порядок.
colors:
  surface: "#fcfcfb"
  surface-container: "#f2f4f3"
  surface-container-high: "#e8ebe9"
  on-surface: "#1c1f1e"
  on-surface-muted: "#5a615e"
  primary: "#2f6b4f"
  on-primary: "#ffffff"
  accent: "#5b3a57"
  on-accent: "#ffffff"
  link: "#1f5fa8"
  error: "#d32f2f"
  outline: "#c9cfcc"
typography:
  display:
    fontFamily: Golos Text
    fontSize: 40px
    fontWeight: 600
    lineHeight: 1.1
    letterSpacing: -0.01em
  heading:
    fontFamily: Golos Text
    fontSize: 30px
    fontWeight: 600
    lineHeight: 1.15
    letterSpacing: -0.005em
  subheading:
    fontFamily: Golos Text
    fontSize: 22px
    fontWeight: 500
    lineHeight: 1.25
    letterSpacing: 0em
  body:
    fontFamily: Golos Text
    fontSize: 17px
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: 0em
  body-sm:
    fontFamily: Golos Text
    fontSize: 15px
    fontWeight: 400
    lineHeight: 1.45
    letterSpacing: 0.005em
  label:
    fontFamily: Golos Text
    fontSize: 14px
    fontWeight: 500
    lineHeight: 1.2
    letterSpacing: 0.01em
rounded:
  none: 0px
  sm: 4px
  md: 8px
spacing:
  xs: 4px
  sm: 8px
  md: 12px
  lg: 16px
  xl: 24px
  2xl: 32px
  3xl: 48px
  4xl: 64px
components:
  hero-title:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    typography: "{typography.display}"
  screen-title:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    typography: "{typography.heading}"
  block-title:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    typography: "{typography.subheading}"
  body-text:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    typography: "{typography.body}"
  hint-text:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface-muted}"
    typography: "{typography.body-sm}"
  button-filled:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    rounded: "{rounded.sm}"
    typography: "{typography.label}"
    padding: "{spacing.lg}"
  button-text:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.primary}"
    rounded: "{rounded.sm}"
    typography: "{typography.label}"
    padding: "{spacing.sm}"
  input:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.on-surface}"
    rounded: "{rounded.sm}"
    typography: "{typography.body}"
    padding: "{spacing.md}"
  option-card:
    backgroundColor: "{colors.surface-container}"
    textColor: "{colors.on-surface}"
    rounded: "{rounded.md}"
    typography: "{typography.body}"
    padding: "{spacing.lg}"
  panel:
    backgroundColor: "{colors.surface-container}"
    textColor: "{colors.on-surface}"
    rounded: "{rounded.md}"
    typography: "{typography.body}"
    padding: "{spacing.xl}"
  panel-nested:
    backgroundColor: "{colors.surface-container-high}"
    textColor: "{colors.on-surface}"
    rounded: "{rounded.sm}"
    typography: "{typography.body-sm}"
    padding: "{spacing.md}"
  badge:
    backgroundColor: "{colors.accent}"
    textColor: "{colors.on-accent}"
    rounded: "{rounded.sm}"
    typography: "{typography.label}"
    padding: "{spacing.sm}"
  link:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.link}"
    typography: "{typography.body}"
  error-text:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.error}"
    typography: "{typography.body-sm}"
  divider:
    backgroundColor: "{colors.outline}"
    rounded: "{rounded.none}"
    height: 1px
  photo:
    backgroundColor: "{colors.surface-container}"
    rounded: "{rounded.none}"
---

# Мастерская

## Overview

Квартира, в которую въехали. Не пустая коробка с эхом и не витрина — жилое
место, где вещи стоят там, где ими пользуются.

Отсюда и весь холст: белый, но мягкий; графит вместо чёрного; блоки отделяются
тоном, а не обводкой; ни одной тени. Ощущение порядка создаётся тем, что
границы почти не видны, а всё равно понятно, где кончается одно и начинается
другое.

Аудитория — человек, который обставляет квартиру, не разбирается в мебели и не
обязан. Он оставляет телефон незнакомому сервису. Интерфейс обязан читаться как
спокойный, аккуратный, предсказуемый: никакой тревоги, никакого давления,
никаких сюрпризов.

Концептуальная основа — Material 3, но взята из неё одна главная идея:
**высота передаётся цветом поверхности, а не тенью**. Тени, градиенты и
стеклянные эффекты не используются вовсе.

## Colors

Пять смыслов, и каждый занят ровно одним цветом. Пересечений нет — это главное
правило палитры, и нарушать его нельзя даже ради красоты.

- **Surface (#fcfcfb)** — мягкий белый, основной фон. Не чистый `#ffffff`:
  чистый белый на экране бьёт по глазам и делает страницу плоской, а лёгкий
  тёплый увод даёт бумагу, а не подсветку.
- **Surface Container (#f2f4f3)** и **Surface Container High (#e8ebe9)** —
  плашки. Две ступени, обе слабые: примерно 3 и 4 по десятибалльной шкале, где
  10 — насыщенный тон. Этого хватает, чтобы блок читался как единое целое, и не
  хватает, чтобы он начал спорить с содержимым.
- **On Surface (#1c1f1e)** — графит, весь основной текст. **16.2:1** на фоне.
  Не чёрный: чёрный на почти белом даёт звон и утомляет на длинных формах.
- **On Surface Muted (#5a615e)** — второстепенный текст: подсказки, подписи,
  счётчики. **6.2:1**, то есть это полноценный читаемый текст, а не
  полупрозрачный шум.
- **Primary (#2f6b4f)** — матовый природный зелёный. **Действие и успех**:
  главная кнопка, подтверждение, пройденный шаг. Белый на нём — **6.3:1**.
- **Accent (#5b3a57)** — слива. **Место креатива и оживления**: шильдик,
  выбранное состояние, акцентная деталь. Никогда не главное действие —
  для действия есть зелёный.
- **Link (#1f5fa8)** — синий. **Только ссылки**, то есть переход в другое
  место. Кнопка синей не бывает никогда.
- **Error (#d32f2f)** — **только ошибка**. Не предупреждение, не акцент,
  не украшение. **4.9:1**, читается как текст.

Нейтральные — графит и белый — уведены в лёгкий зелёный подмес. Серый без
подмеса читается как несделанный выбор; серый, склонённый к акценту, читается
как выбранный.

**Слива и красный оба красноватые — их разводит дисциплина, а не оттенок.**
Слива всегда тёмная заливка на площади и никогда не интерактивна. Красный
всегда тонкая линия или текст и всегда означает «здесь не так». Если хочется
покрасить сливой кнопку — это сигнал, что кнопка не нужна.

## Typography

**Golos Text, шкала весов 400 / 500 / 600.** Гротеск с кириллицей в основе,
бесплатный, с живыми начертаниями.

| Роль | Размер | Вес | Где |
|---|---|---|---|
| display | 40 | 600 | первый экран лендинга, одна штука на страницу |
| heading | 30 | 600 | заголовок экрана |
| subheading | 22 | 500 | заголовок блока |
| body | 17 | 400 | основной текст |
| body-sm | 15 | 400 | подсказки, подписи, счётчики |
| label | 14 | 500 | кнопки, метки, шильдики |

**Регистр предложный везде.** Капса нет ни в навигации, ни на кнопках, ни в
метках. На кириллице прописные съедают выносные элементы, по которым глаз
узнаёт слово целиком, и текст приходится читать по буквам.

Трекинг у крупного отрицательный, у мелкого слабо положительный: крупные
кегли иначе рассыпаются, мелкие — слипаются.

Связный текст держать около 65 знаков в строке. Выравнивание левое.

## Layout

Одна колонка, непрерывная прокрутка, максимум 1440px. Боковое поле не меньше
16px на любой ширине.

Шкала отступов кратна четырём: 4 · 8 · 12 · 16 · 24 · 32 · 48 · 64.
Между блоками 32–48, внутри блока 12–16, между строкой и её подписью 4–8.

**Расстояние — главный инструмент группировки.** Прежде чем ставить плашку,
проверить, не решается ли задача пустым местом: если между блоками 48, а внутри
блока 12, группировка уже видна и плашка не нужна.

Плотность спокойная. Пустое место — рабочий материал.

Липкой шапки нет. Модальных окон нет: шаг, который хочется показать модалкой,
показывается стадией той же страницы.

## Elevation & Depth

**Теней нет. Ни одной.** Градиентов нет, стеклянных эффектов нет,
приподнятых поверхностей нет.

Уровней три, и все они — цвет:

| Уровень | Чем выражен | Что это |
|---|---|---|
| 0 | `surface` | холст страницы |
| 1 | `surface-container` | блок, объединённый по смыслу |
| 2 | `surface-container-high` | вложенное в блок: поле, строка, выбранный вариант |

Глубже второго уровня не заходить. Третья вложенность означает, что блок
спроектирован неверно и его надо разбить.

**Когда плашка нужна и когда нет.** Плашка обязана нести смысл «это одно
целое». Она уместна, когда несколько разнородных элементов образуют группу —
вопрос с вариантами ответа, сводка ответов, карточка мастерской. Она не нужна,
когда элемент и так самостоятелен: заголовок, абзац, фотография, одиночная
кнопка. Плашка вокруг того, что и так читается как единое, — шум.

**Обводки блоков не применяются.** Поле ввода — единственное исключение: у него
граница есть, потому что она обозначает место, куда можно писать.

## Shapes

Три ступени радиуса, и каждая закреплена за своим классом объектов.

| Радиус | Где |
|---|---|
| `0` | фотографии, разделители, линии сетки |
| `4` | кнопки, поля ввода, шильдики |
| `8` | карточки, плашки, всплывающие окна |

**Фотографии всегда с нулевым радиусом.** Скруглённый угол на снимке мебели
отрезает предмет и уводит масштаб; прямой угол передаёт текстуру и размер.

**Правило вложенности.** Дуги вложенных углов обязаны быть концентричны:
радиус внутреннего элемента = радиус внешнего − отступ между ними. Кнопка
с радиусом 4 внутри карточки с радиусом 8 и отступом 16 выглядит верно; кнопка
с радиусом 8 внутри той же карточки — нет, её угол спорит с углом карточки.

Радиус больше 8 не заводить. Пилюль и полностью круглых элементов в системе
нет, кроме аватара, если он появится.

## Components

- **Кнопка основная** — зелёная заливка, белая надпись, радиус 4. Одна главная
  кнопка на экран. Если главных действий два, значит экран делает два дела
  и его надо разделить.
- **Кнопка-текст** — зелёная надпись без заливки, радиус 4. Второстепенное
  действие рядом с главным.
- **Разделитель** — линия 1px цветом `outline`, во всю ширину контента.
  Применяется там, где расстояния между блоками не хватает.
- **Поле ввода** — белый фон, граница 1px цветом `outline`, радиус 4. Состояния меняют
  цвет границы, но не её толщину. Ошибка — граница красная, сообщение под полем
  красным, на 15px.
- **Карточка выбора** — плашка первого уровня, радиус 8. Выбранная поднимается
  на второй уровень и получает сливовую метку. Заливки всей карточки цветом
  нет: она бы конкурировала с кнопкой.
- **Плашка** — первый уровень, радиус 8, внутренний отступ 24.
- **Вложенная плашка** — второй уровень, радиус 4, отступ 12.
- **Шильдик** — сливовая заливка, белая надпись, радиус 4. Короткое слово,
  не предложение.
- **Ссылка** — синяя, с подчёркиванием. Подчёркивание не убирать: цвет один
  не отличает ссылку для тех, кто плохо различает оттенки.
- **Фотография** — радиус 0, без рамки. На первом экране лендинга допускается
  во всю ширину, за поля колонки.

### Состояния

- **Наведение** — затемнение заливки на 8% у кнопок, подчёркивание у ссылок.
- **Нажатие** — затемнение на 12%, без сдвига и без изменения размера.
- **Фокус** — контур 2px цветом `primary` со смещением 2px. Появляется
  мгновенно, без анимации. Не убирать никогда.
- **Недоступно** — непрозрачность 38%, курсор запрета, атрибут `disabled`.
  Тремя каналами сразу.
- **Ошибка** — красная граница поля и красный текст под ним. Иконка не нужна:
  текст называет причину точнее.
- **Выбрано** — второй уровень поверхности плюс сливовая метка.

### Иконки

Один набор, один стиль: **контурные, линия 1.5px, скруглённые концы, размер
20 или 24**. Рисуются вручную, никаких сборных наборов из разных источников.

- **Иконки действий** — стрелка, поделиться, закрыть, назад. Только там, где
  действие повторяется и его узнают по форме. Иконка без подписи допустима
  только у стрелки и закрытия.
- **Иконки категорий** — кухня, шкаф, ванная, спальня. Рисуются как предметные
  контуры в одном стиле, одной толщиной, в одной перспективе. Это единственное
  место, где иконка несёт содержание, а не украшает.
- **Смысловые в блоках** — только если блок без иконки читается хуже.
  Эмодзи в интерфейсе не используются.

### Affordance

Кликабельное обязано выглядеть кликабельным, но не кричать об этом.

Достаточно одного признака: заливка у кнопки, подчёркивание у ссылки, граница
у поля. Два признака сразу — уже перебор: кнопка с заливкой, обводкой и тенью
выглядит как заготовка, а не как решение.

Некликабельное не должно притворяться. Плашка не реагирует на наведение,
если по ней нельзя нажать.

## Content

Текст — часть системы, а не начинка.

- **Надпись у действия говорит о действии.** «Продолжить», «Отправить заявку»,
  «Открыть предложения». Не «Далее», не «Ок», не «Отправить» без объекта.
- **Коротко и по делу.** Нужно действие — пишем про действие. Нужно подсветить
  шаг — подсвечиваем один шаг. Биографию не расписываем.
- **За ручку не ведём.** Человек взрослый. Объяснение появляется там, где без
  него не сделать шаг, и нигде больше.
- **Вопросы не по теме блока уходят в FAQ.** Если в блоке напрашивается абзац
  «а что если…», это признак, что абзац не отсюда. Блок отвечает за одно
  действие, остальное — отдельным разделом.
- **Названия материалов и фурнитуры заказчику не показываются.** Ни ЛДСП,
  ни МДФ, ни марок. Это язык мебельщика, не покупателя.
- **Язык интерфейса русский.** Тексты живут в `src/texts` и `src/questions`.

## Presence

Ощущение присутствия — то, ради чего всё остальное. Оно не берётся из
компонентов; оно берётся из деталей, и вот из каких именно.

- **Настоящие фотографии с обжитыми сценами.** Не пустой гарнитур в вакууме,
  а кухня, в которой живут. Пустой предмет на белом — это каталог поставщика,
  а не квартира.
- **Настоящие числа и имена.** «Вилка 900 000 – 1 200 000 ₸», «Марат, цех
  в Алматы» — конкретика читается как присутствие, обобщение как заглушка.
- **Человеческие формулировки в мелочах.** «Померьте стену, где встанет кухня»
  вместо «Укажите линейный размер». Падежи согласованы: «3,2 метра», а не
  «3,2 метров». Такие мелочи и создают ощущение, что по ту сторону живой
  человек.
- **Следы процесса, а не только результата.** Срок, этап, кто ответил —
  движение показывает, что здесь работают.
- **Пустые состояния говорят словами.** «Мебельщики ещё не ответили» вместо
  пустого места или серого прямоугольника.

## Do's and Don'ts

### Do

- Разделять блоки тоном и расстоянием.
- Держать одно значение цвета на один смысл: зелёный — действие, синий —
  ссылка, красный — ошибка, слива — акцент.
- Ставить фотографии с нулевым радиусом.
- Соблюдать концентричность вложенных радиусов.
- Держать одну главную кнопку на экран.
- Проверять, не решается ли группировка пустым местом, прежде чем ставить плашку.
- Писать надпись кнопки про действие, которое произойдёт.

### Don't

- Никогда не добавлять тени, градиенты и стеклянные эффекты.
- Никогда не обводить блоки рамкой — граница есть только у поля ввода.
- Никогда не красить кнопку синим, сливовым или красным.
- Никогда не использовать красный ни для чего, кроме ошибки.
- Никогда не набирать интерфейс капсом.
- Никогда не вкладывать поверхности глубже второго уровня.
- Никогда не скруглять фотографии.
- Никогда не ставить эмодзи вместо иконок.
- Никогда не показывать заказчику названия материалов и фурнитуры.
