// Иконки категорий — единственное место системы, где иконка несёт содержание,
// а не украшает (DESIGN.md § Иконки). Рисуются вручную, скруглёнными концами,
// в одной фронтальной перспективе.
//
// Третий заход. Первый провалился из-за решётки из шести линий на 24
// пикселях, второй — из-за того, что голый силуэт не опознаётся: тумба
// с раковиной читалась как кастрюля. Размер поднят до 32, толщина до 1.75
// (§ Иконки), и у каждого предмета появилась деталь, по которой его узнают:
// конфорки у плиты, кран у раковины, штанга в шкафу, ручки у комода.
//
// Перспектива фронтальная у всех четырёх. Смешивать вид спереди и сверху
// нельзя — набор сразу рассыпается; планировки кухонь рисуются сверху и
// поэтому живут отдельным компонентом KitchenShape.

// Тип берётся из ядра, а не повторяется здесь: добавится категория —
// компилятор потребует иконку, а не промолчит.
import type { CategoryId } from '../questions/categories'

export function CategoryIcon({ id }: { id: CategoryId }) {
  return (
    <svg viewBox="0 0 32 32" aria-hidden="true" className="size-8 shrink-0"
      fill="none" stroke="currentColor" strokeWidth="1.75"
      strokeLinecap="round" strokeLinejoin="round">
      {/* Кухня: навесной шкаф, столешница, варочная панель с конфорками. */}
      {id === 'kitchen' && (
        <>
          <path d="M18 4h10v8H18z" />
          <path d="M3 17h26" />
          <path d="M4 17v11h24V17" />
          <circle cx="10" cy="22" r="1.6" />
          <circle cx="16" cy="22" r="1.6" />
          <path d="M22 21.5h4" />
          <path d="M22 25h4" />
        </>
      )}
      {/* Шкаф: корпус, две створки, штанга с плечиками и ручки у стыка. */}
      {id === 'wardrobe' && (
        <>
          <path d="M6 3h20v26H6z" />
          <path d="M16 3v26" />
          <path d="M14.2 14v3.5" />
          <path d="M17.8 14v3.5" />
          <path d="M9.5 8.5h3.2" />
        </>
      )}
      {/* Ванная: раковина с краном на тумбе с двумя дверцами. */}
      {id === 'bathroom' && (
        <>
          <path d="M16 4v5" />
          <path d="M16 6h4.5v3" />
          <ellipse cx="16" cy="13" rx="8" ry="3" />
          <path d="M6 15h20v14H6z" />
          <path d="M16 18v11" />
          <path d="M13 21h-1.5" />
          <path d="M19 21h1.5" />
        </>
      )}
      {/* Другое: комод — три ящика с ручками, как в подсказке категории. */}
      {id === 'other' && (
        <>
          <path d="M4 6h24v22H4z" />
          <path d="M4 13.3h24" />
          <path d="M4 20.6h24" />
          <path d="M14 9.7h4" />
          <path d="M14 17h4" />
          <path d="M14 24.3h4" />
        </>
      )}
    </svg>
  )
}
