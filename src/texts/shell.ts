// ОБОЛОЧКА. Тексты навигации и подвала — те, что видит человек на каждой
// странице. Живут здесь, а не в компоненте: их правят по итогам
// дозвонов, и искать формулировку в разметке не должно приходиться (CLAUDE.md,
// «язык интерфейса русский, тексты в src/texts»).

/** Шапка и подвал PageShell. Регистр предложный — капса в системе нет. */
export const shell = {
  /**
   * Имя проекта. Решение PM от 16.09: до этого в макетах стояла «мастерская»,
   * а в документах названия не было вовсе — открытый вопрос хэндовера закрыт.
   * Пишется с прописной: это имя, а не подпись.
   */
  brand: 'Капибара',
  /**
   * Ссылка на лендинг из подвала. Главная с 20.09 — каталог, и рассказ
   * о том, как устроен сервис, стало неоткуда открыть: реклама ведёт
   * на /promo напрямую, а пришедший в каталог про сервис не узнаёт ничего.
   */
  howItWorks: 'Как это работает',
  /**
   * Вход мебельщика. Стоит в подвале, а не в шапке: так делают и Bark,
   * и Naimi — шапка принадлежит заказчице. Ведёт на ту же дверь `/login`,
   * роль определяет сервер по номеру, и заказчице выбирать не из чего.
   */
  masterEntry: 'Вход для мастерских',

  nav: {
    /**
     * Вход и кабинет — один пункт, который меняет имя по состоянию
     * (правка 20.09). Каталог открыт без входа: человека, пришедшего
     * смотреть мастерские, никто не просит представиться.
     */
    signIn: 'Войти',
    cabinet: 'Кабинет',
    request: 'Заявка',
    masters: 'Мастерские',
  },
  /** Подпись терминальной полосы: города, где заявку есть кому передать. */
  band: 'Мастерские Алматы, Астаны и Шымкента',
} as const

// PROBE: заглушка лендинга — экран US-01 в срезе 1 ещё не сделан, а точка
// входа «/» должна вести к форме руками, иначе сквозной проход не пройти.
// Удаляется вместе с заглушкой, когда появится настоящий лендинг.
export const landingStub = {
  title: 'Мебель на заказ',
  body:
    'Лендинг ещё не собран — это US-01. Форма заявки уже работает: ' +
    'ссылка «Заявка» в шапке.',
  toRequest: 'Оставить заявку',
} as const
