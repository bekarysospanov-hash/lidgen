# Точка входа: следующая сессия — дизайн-система, потом каталог и состав КП

**Обновлено:** 17.09.2026
**Состояние:** **срезы 1 и 2 закрыты кодом целиком**, из среза 3 сделаны
US-19b, US-22, US-23, US-24 · весь путь пройден PM руками и прогнан
регрессом в 26 шагов · всё в `main`, `origin/main` не отстаёт ·
281 тест, четыре контура зелёные

> **Сессии называются по теме и не смешиваются** (решение PM, 16.09).
> Эта закрыта на «срез 2 + ревизия концепции». Следующая — **правка
> `DESIGN.md`**: она названа причиной того, что интерфейс «выглядит
> как вёрстка», и лезть в экраны до неё бессмысленно.

## Первая команда сессии

```bash
cd /Users/aida/lidgen && npm run design:check && npm run design:lint && npm test && npm run dev
```

Четыре контура должны быть зелёными до первой строки кода: `design:check`
чисто · `design:lint` 0/0 · **281 тест** · `tsc` exit 0.

---

## Что решено на ревизии 17.09 — читать до того, как что-то менять

Вопрос PM звучал так: «как клиент поймёт, что помимо заявки здесь можно
посмотреть других мебельщиков». Разбор показал, что в голове держались
**две разные модели продукта**, и половина непонимания шла оттуда.

| Решение | Суть | Почему так |
|---|---|---|
| **Каталог — витрина доверия** | отвечает на «здесь живые люди?» и ни на что больше | прямой контакт уводит сделку мимо продукта, и метрика «дошло до КП» становится нижней границей: падение заявок не отличить от провала канала |
| **Контактов в карточках нет** | связаться напрямую нельзя | то же самое; контакты — второй такт, вместе с адресной заявкой |
| **Заявка остаётся веерной** | из карточки можно перейти к форме, но уйдёт она всем на приёме | так записано в PRD US-03 с самого начала; рамка канваса — лидген, не маркетплейс |
| **Под карточкой — «Оставить заявку» и строка про веер** | «Заявку получат несколько мастерских, не только эта» | строка стоит ничего, а без неё человек узнаёт правду на дозвоне — в худшей точке |
| **Состав КП станет структурой** | `items` + `extra` + обязательное `excluded` | два свободных текста рядом сравнить нельзя: сравнивается многословность, а не предложения. Подробности и риск — `api-contract.md` §9 |
| **«Неконсистентность» дизайна — дыра в `DESIGN.md`** | не в экранах | см. ниже, с числами |

Первые четыре записаны в `prd-lidgen.md` рядом с US-02 и US-03, пятое —
в `api-contract.md` §9 и журнал изменений. Шестое — задача этой сессии.

---

## Почему интерфейс «выглядит как вёрстка» — диагноз с числами

`design:check` чист, нарушений нет — **потому что нарушать нечего**.
Система задаёт шкалу значений, но не задаёт **связки**, и где нет правила,
там решение принимается заново на каждом экране:

```
ширина связного текста:   62ch × 22 · 58ch × 13 · 54ch × 6   ← один и тот же тип текста
отступ заголовок→абзац:   mt-lg · mt-md · mt-sm              ← одна и та же пара
между секциями:           mt-3xl × 22 из 23                  ← здесь правило ЕСТЬ, и ровно
```

Правый край текста гуляет от экрана к экрану — глаз ловит именно это.
Плюс на широком экране колонка не ограничена ничем, кроме `62ch`,
и текст висит слева в пустоте. Единой «шапки экрана» тоже нет: где-то
есть надстрочная метка, где-то нет.

**Чинить правкой экранов нельзя** — следующий экран снова выберет число
наугад. Лечится правилом в файле.

---

## Что сделано после первой правки этого файла

**Дизайн-система пригвождена** (`78eb945`): мера текста одна — 41rem, это
67 знаков, измерено на живой странице · мера заголовка 23rem · колонка 720
отделена от страницы 1440 · шесть связок ритма закреплены числами · цель 44px
с названной границей · всё в rem вместо px · токены иконок и чертежей ·
прозой четыре принципа и два записанных противоречия.

**`design:check` закрыл четыре дыры:** числовые утилиты Tailwind мимо шкалы,
произвольные значения в скобках по белому списку, дефолтная палитра,
инлайновые стили. Проверено подложенным файлом с нарушениями — ловит все.

**Playwright: сквозной путь закреплён файлом** (`ff62913`). `npm run e2e` —
20 секунд вместо двадцати минут. Он же кладёт снимки в `e2e/shots/`, и они
доходят до PM файлами: панель браузера дважды за день отдавала пустые кадры.
Тест нашёл три дефекта доступности, не замеченных четырьмя ручными проходами.

---


## Порядок следующей сессии

1. **Состав КП структурой** (§9 контракта): контракт → схемы → моки →
   форма мебельщика чипсами + обязательное «что не входит» → таблица
   сравнения матрицей «у кого что есть». Единственное, что можно сделать
   кодом без материалов от PM
2. **Усилить `DESIGN.md` там, где он молчит** (решено на ревизии 17.09):
   **чек-лист экрана** в конце файла — десять строк «проверь перед сдачей»,
   составленных не из головы, а из ошибок, реально сделанных 16–17.09:
   плашка с заголовком внутри, переопределение компонента на месте,
   шильдик-дубль, подсказка на языке цеха, четыре ширины текста.
   И **строка про молчание проверки**: чистый `design:check` не означает,
   что экран сделан правильно, — только что машинные правила не нарушены
3. **Четыре новых правила в `design:check`**, все однозначные: вложенность
   поверхностей глубже двух уровней · эмодзи в разметке и текстах · снятый
   фокус (`outline-none`) · две главные кнопки на одном экране
4. **Список детекторов Impeccable** (github.com/pbakaus/impeccable, 61 штука) —
   вытащить те правила, которых у нас нет. Инструмент не ставить: команды
   генерации (`craft`, `shape`, `animate`, `polish`) несут своё представление
   о красоте и будут спорить с системой. Ставить — только `critique`
   и `audit`, с тем же ограничением, что записано про hallmark
5. **Экраны до второй редакции компонентов:** `RequestSent`, `Offers`,
   `Landing`. Плюс шапки экранов: правило «метка → заголовок → лид» введено,
   а экраны под него не выровнены
6. **Каталог с реальными карточками**, когда придут согласия и фото (A2)
7. `/review` · `npm run e2e` · показ PM по правилу выше

---

## Что сделано в этой сессии

**Срез 1 достроен целиком:** контракт кабинета (§5б, пять операций,
`masterToken`, `Master`, `Routing`), схемы, моки, четыре экрана — вход
по номеру и коду, список заявок, карточка с формой ответа, страница
предложений заказчицы. «Момент истины скелета» наступает.

**Срез 2 закрыт кодом:** US-11 согласие на ПДн (обязательное в схеме,
страница `/privacy` с версией) · US-08 отделка, срок, ЖК · US-06 двери
и высота шкафа чертежами · US-07 проверена, правок не потребовала ·
US-01 визит с метками и переход с лендинга · US-02 каталог механикой ·
US-25a/US-26 воронка и UTM · US-09 фронтовая часть плюс запись в §9,
почему операторского контура здесь быть не должно.

**Срез 3, четыре истории:** US-22 накопление, US-23 сравнение таблицей,
US-24 контакт по нажатию, US-19b правка КП вместо второго предложения.

**Три ревью, все находки закрыты.** Самое ценное: циклы импорта в моках,
вход без запроса кода, подсказка с телефонами на публичном экране,
`contact_made` без привязки к заявке, потеря событий при закрытии вкладки,
токен в `referrer`.

**Найдено PM при проходе и починено:** знак в кабинете не вёл на главную ·
из кабинета не было выхода вообще, и войти второй мастерской было нельзя.

---

## Долги и непроверенное

- **Утилизация проверена частично.** Прод-сборка с `VITE_USE_MOCKS=false`
  собирается, и пробных данных в ней нет — ни телефонов, ни демо-заявок,
  ни подсказок. Но **сам код PROBE из исходников ни разу не вырезали**:
  26 меток в 18 файлах, `grep -rn "PROBE:" src`
- **`http.ts` по-прежнему не видел живого сервера** — четырнадцать операций
  против фейкового `fetch` в тестах
- **Иконки навигации не проверены на человеке со стороны.** Витрина для
  «Мастерских» — догадка; на ревизии предложено убрать её и оставить
  подписанную ссылку, решения нет
- **Два коммита с испорченными сообщениями** (`2c832ec`, `12c8d9d`):
  в текст затянуло скрипт сборки превью. Уже на `origin`; переписывать
  историю ради этого дороже, чем оставить
- **Показывать ли заказчице, каким мастерским ушла заявка** — предложено,
  не решено. Контракт этого не отдаёт

---

## Решения, не переоткрывать без новых фактов

| Решение | Где |
|---|---|
| Проблема — прозрачность цены, носитель заказчик | канвас |
| Рамка — лидген-эксперимент, не маркетплейс целиком | канвас |
| Каталог — витрина доверия, контактов в нём нет | PRD, US-02, 17.09 |
| Заявка веерная даже из карточки мастерской | PRD, US-03 |
| Бюджет-вилку у заказчицы не спрашиваем | решение PM, 16.09 |
| Телефон заказчицы открывается мебельщику только после КП | контракт §7 |
| Телефон мастерской приходит вместе с её КП | контракт §2, US-24 |
| Правка КП вместо второго предложения | контракт §5б, US-19b |
| Согласие на ПДн проверяется схемой, а не только экраном | контракт §5, US-11 |
| Сгенерированных карточек в каталоге не будет | PRD US-02, контракт §2 |
| Контракт меняется первым, потом мок, потом экран | `CLAUDE.md` |
| Весь UI — через `/ui-build`, `DESIGN.md` правится отдельной сессией | `CLAUDE.md` |

## Открытые вопросы

| Вопрос | Владелец |
|---|---|
| **Пропуски в политике**: оператор, **БИН** (у ИП — ИИН), срок хранения, адрес для отзыва | PM + юрист, блокирует GATE 1 |
| **Локализация ПДн в РК** — можно ли держать телефоны за границей | PM, блокирует выбор хостинга |
| Карточки мастерских: согласия, тексты, свои фото работ | маркетолог, A2 |
| Реальные снимки для лендинга вместо временных | маркетолог, A1 |
| Кто дежурит на дозвоне и в какие часы | маркетолог |
| Числа политики OTP в контракте | PM, до передачи разработчику |
| Канал уведомлений: WhatsApp, Telegram или оба | PM + разработчик |
| Срок удаления данных участников | PM, на GATE 1 |

---

## Чего эксперимент не ответит

Дойдёт ли заявка до сделки — цикл мебели 3–8 недель, тест до неё не доживёт.
На 15 заявках это проверка существования сигнала, а не статистика рынка.

**И главное, что не решается кодом:** без 3+ мебельщиков на приёме запуска
нет, сколько бы экранов ни было написано. A1 — единственный критический путь,
и он на маркетологе. Карточки каталога и фотографии работ — оттуда же.
